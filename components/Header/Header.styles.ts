import styled from 'styled-components';

export const Wrapper = styled.header`
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1140px;
    padding: 2rem 1rem;
    background: linear-gradient(0deg, rgba(1,10,0,0) 0%, rgba(1,10,0,0.9) 40%, rgba(1,10,0,1) 80%);
`;

export const Nav = styled.nav`
    display: flex;
    gap: 2rem;
`